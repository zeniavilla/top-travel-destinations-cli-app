require "spec_helper"

RSpec.describe TopTravelDestinations do
  it "has a version number" do
    expect(TopTravelDestinations::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
